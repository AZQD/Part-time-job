opera mini:（默认为无痕浏览）
1.window.localStorage不兼容；
2.不支持line-height；
3.很多动效不支持；
4.对ES6不支持；
5.对引入评论js的下面这行代码不支持；
(d.head || d.body).appendChild(s);
6.不能同时弹出多个alert；
7.对text-indent属性不支持；
8.对上传图片input type=file不支持；
9.圆角；（用插件试试）

/*
    * window.localStorage.getItem  getCookie
    *
    * localStorage.setItem  setCookie
    * localStorage.removeItem  delCookie
    *
    *
    * */

    //无痕模式
    /*
    * localStorage不可用，可用cookie
    * //opera mini
     /*try {
     window.localStorage.foobar = "foobar";
     } catch (_) {
     alert("请取消浏览器无痕浏览再购买哦~");
     }*/
    //alert(window.sessionStorage);